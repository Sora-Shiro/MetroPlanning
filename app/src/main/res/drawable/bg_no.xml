<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 手指触摸时的button -->
    <item android:drawable="@drawable/no_dark"
          android:state_pressed="true"/>

    <!-- 默认的（触摸之前）button -->
    <item android:drawable="@drawable/no_light"/>

</selector>